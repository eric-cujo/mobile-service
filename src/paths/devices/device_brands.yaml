get:
  tags:
    - Devices API
  summary: Query device brands
  description: "To query device brands perform the following request.

  \r\n  Parameter descriptions -

\r\ntype - if empty will be ignored, else will match exact type names.

\r\nmodel - if empty will be ignored, else will match exact models.

\r\nbrand - will check for brands that contain the provided string."
  operationId: QueryDeviceBrands
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref:
          examples:
            device:
              value:
                id: tbd
