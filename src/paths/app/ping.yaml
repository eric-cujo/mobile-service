post:
  tags:
    - App API
  summary: Ping the cloud urls
  description: Ping the cloud urls
  operationId:
  requestBody:
    content:
      application/json:
        schema:
          $ref: 
  responses:
    204:
      description: (204 means that the cloud was successfully pinged. "The server has successfully fulfilled the request and that there is no content to send in the response payload body."")
      content:
        application/json:
          schema:
            $ref:
