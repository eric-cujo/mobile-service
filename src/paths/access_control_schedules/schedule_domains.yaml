get:
  tags:
    - Access Control Schedules
  summary: Get domains for schedule
  operationId: GetDomainsforSchedule
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
            $ref:
          examples:
              weekDay:
                value:
                  domains: TBD

put:
  tags:
    - Access Control Schedules
  summary: Update domains for schedule
  operationId: UpdateSchedule
  requestBody:
    content:
      application/json:
        schema:
          $ref:
        examples:
          features:
            value:
              domains: tbd
    required: true
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
            $ref:
          examples:
            features:
              value:
                domains: tbd
