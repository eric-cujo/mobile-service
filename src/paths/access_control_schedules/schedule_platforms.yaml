get:
  tags:
    - Access Control Schedules
  summary: Get platforms for schedule
  operationId: GetPlatformsforSchedule
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
            $ref: "/src/swagger.yaml#/components/schemas/ScheduleResponse"
          examples:
              weekDay:
                value:
                  weekDay: tue
                  startTime: tue
                  endTime: tue
                  title: tue
                  enabled: tue
                  pauseInternet: tue

put:
  tags:
    - Access Control Schedules
  summary: Update platforms for schedule
  operationId: UpdatePlatformsforSchedule
  requestBody:
    content:
      application/json:
        schema:
          $ref:
        examples:
          features:
            value:
              blocked: tbd
    required: true
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
            $ref:
          examples:
            features:
              value:
                blocked: tbd
