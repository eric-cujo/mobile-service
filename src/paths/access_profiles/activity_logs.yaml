get:
  tags:
    - Access Profiles
  summary: Get blocked domains by access profile id and additional query parameters.
  description: Get blocked domains by access profile id and additional query parameters.
  operationId: GetBlockedDomainsbyAccessProfile
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
              $ref: "/src/swagger.yaml#/components/schemas/AccessProfileActivityLogs"
          examples:
            schedule:
              value:
                profileName:
                domain:
                categories:
                fromDays:
                toDays:
