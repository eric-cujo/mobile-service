post:
  tags:
    - Device Groups API
  summary: Reposition device groups
  description: Change position of a device group.
  operationId: RepositionDeviceGroups

  responses:
    200:
      description: Deleted device group successfully.
      content:
        application/json:
          schema:
              $ref: "/src/swagger.yaml#/components/schemas/DeviceGroupPosition"
          examples:
