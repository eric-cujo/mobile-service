post:
  tags:
    - Users API
  summary: Logout
  description: Logout.
  operationId: Logout
  requestBody:
    content:
      application/json:
        schema:
          $ref:
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref:
          examples:
            schedule:
              value:
                - state:
