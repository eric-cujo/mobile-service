get:
  tags:
    - Agents API
  summary: Get mode of agent
  description: Get mode of a specific agent by id.
  operationId: GetModebyID
  requestBody:
      description:
      content:
        application/json:
          schema:
              $ref: "/src/swagger.yaml#/components/schemas/AgentModeID"
          examples:
            schedule:
              value:
                  mode: DHCP
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
              $ref: "/src/swagger.yaml#/components/schemas/AgentList"
          examples:
            schedule:
              value:
                  id: TBD
                  selected:
                  description:
                  name:
                  icon:
