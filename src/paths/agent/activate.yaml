post:
  tags:
    - Agents API
  summary: Once logged in, in order to register an agent to a customer post to the following.
  description: Get the list of agents for the logged in customer.
  operationId:
  requestBody:
    description: Request body should contain MAC address of the router or which is connected to both the mobile device and the agent.
    content:
      application/json:
        schema:
          $ref: "/src/swagger.yaml#/components/schemas/AgentActivateReq"
        examples:
          newName:
            value:
              mac: aabbccddeeff
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
              $ref:
          examples:
            agent:
              value:
                - mac: TBD
