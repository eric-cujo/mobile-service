get:
  tags:
    - Agents API
  summary: Re-scan agent devices
  description: Re-scan device for specified agent.
  operationId: RescanDevices
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
              $ref:
          examples:
            schedule:
              value:
                 id: TBD
