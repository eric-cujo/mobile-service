get:
  tags:
    - Agents API
  summary: Setup agent
  description: Gets agent setup status and begins firmware update if needed.
  operationId: SetupAgent
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
              $ref: "/src/swagger.yaml#/components/schemas/AgentSetupResp"
          examples:
            schedule:
              value:
                  retryInSeconds: TBD
                  message:
