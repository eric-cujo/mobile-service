get:
  tags:
    - Agents API
  summary: Get firmware schedule information
  description: Get information about currently scheduled firmware update or firmware update reminder
  operationId: GetFirmwareScheduleInfo
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
              $ref: "/src/swagger.yaml#/components/schemas/AgentList"
          examples:
            schedule:
              value:
                - id: TBD
                  selected:
                  description:
                  name:
                  icon:
