post:
  tags:
    - Events API
  summary: Block Threat
  description: Block event (threat) if it is not in blocking mode.
  operationId:

  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref:
