get:
  tags:
    - Events API
  summary: Get list of events for a specified agent.
  description: Get list of events for a specified agent which occurred during the specified amount of days.
  operationId:
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
            $ref: 
          examples:
            device:
              value:
                id: tbd
