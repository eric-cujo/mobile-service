post:
  tags:
    - Events API
  summary: Mark all events as read.
  description: Mark all events as read.
  operationId: MarkAllasRead

  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref:
