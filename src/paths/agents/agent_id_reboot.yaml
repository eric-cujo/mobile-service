post:
  tags:
    - Agents API(PA)
    - GENERAL/CORE(PA)
  summary: Reboot the agent
  description: Initiate the agent reboot
  operationId: RebootTheAgent
  requestBody:
    description: Number of seconds.
    content:
      application/json:
        schema:
          $ref: "/src/swagger.yaml#/components/schemas/RebootAgentRequest"
        examples:
          delay:
            value:
              delayTime: 360
  responses:
    204:
      description: Reboot
  security:
    - AccessToken: []
parameters:
  - $ref: "/src/swagger.yaml#/components/schemas/AgentIdOrPartnerAgentIdParameter"
