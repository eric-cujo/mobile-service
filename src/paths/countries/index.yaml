get:
  tags:
    - Countries API(PA)
    - SECURITY (PA)
  summary: Get all available countries (POC)
  description: Retrieve all available countries
  operationId: GetAllAvailableCountries
  responses:
    200:
      description: OK.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "/src/swagger.yaml#/components/schemas/Country"
          examples:
            countries:
              value:
                - name: Some name
                  id: 123
  security:
    - AccessToken: []
