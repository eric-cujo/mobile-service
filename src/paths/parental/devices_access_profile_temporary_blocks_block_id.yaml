delete:
  tags:
    - PARENTAL LOW-LEVEL(PA) API
  summary: Remove a temporary blocking entry
  parameters:
    - $ref: '/src/swagger.yaml#/components/schemas/DeviceIdParameter'
    - name: blockId
      in: path
      required: true
      style: simple
      explode: false
      schema:
        type: integer
  responses:
    200:
      description: OK
